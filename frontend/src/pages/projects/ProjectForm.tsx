import { Typography } from 'antd';

const { Title } = Typography;

export default function ProjectForm() {
  return (
    <div>
      <Title level={2}>Add New Project</Title>
      <p>Coming soon...</p>
    </div>
  );
}
